# File Toolbox Configuration
# Copy this file to .env and adjust values as needed

# Server Configuration
PORT=3000
BUFFER_SIZE=10485760  # 10MB in bytes

# Worker Pool Configuration
NUM_WORKERS=5
JOB_TIMEOUT=30s
WORKER_RESULT_TIMEOUT=1s
JOB_QUEUE_TIMEOUT=100ms
JOB_QUEUE_SIZE=100

# File System Configuration
UPLOAD_PATH=./uploads

# Example duration formats:
# - 30s (30 seconds)
# - 1m (1 minute)
# - 100ms (100 milliseconds)
# - 1h30m (1 hour 30 minutes)
