name: Docker Scan

on:
  repository_dispatch:
    types: [docker-build-complete]

env:
  REGISTRY: docker.io
  IMAGE_NAME: antonbiz/file-toolkit

jobs:
  scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    
    steps:
    - name: Determine image tag to scan
      id: tag
      run: |
        # Extract the first tag from the tags string
        TAGS="${{ github.event.client_payload.image-tags }}"
        FIRST_TAG=$(echo "$TAGS" | tr ' ' '\n' | head -1)
        echo "tag=$FIRST_TAG" >> $GITHUB_OUTPUT
        echo "Will scan: $FIRST_TAG"

    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: ${{ steps.tag.outputs.tag }}
        format: 'sarif'
        output: 'trivy-results.sarif'

    - name: Upload Trivy scan results to GitHub Security tab
      uses: github/codeql-action/upload-sarif@v2
      if: always()
      with:
        sarif_file: 'trivy-results.sarif'

    - name: Trigger push workflow
      if: success() && github.event.client_payload.ref != 'refs/pull/'
      uses: peter-evans/repository-dispatch@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        event-type: docker-scan-complete
        client-payload: |
          {
            "image-digest": "${{ github.event.client_payload.image-digest }}",
            "image-metadata": ${{ github.event.client_payload.image-metadata }},
            "image-tags": "${{ github.event.client_payload.image-tags }}",
            "ref": "${{ github.event.client_payload.ref }}",
            "sha": "${{ github.event.client_payload.sha }}"
          }
